/** @type {import('next').NextConfig} */
const nextConfig = {
  // generateStaticParams: async function () {
  //   return {
  //     '/': { page: '/' }
  //   }
  // }
};

export default nextConfig;
